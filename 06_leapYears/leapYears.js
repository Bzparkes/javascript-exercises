const leapYears = function(years) {
    let yearCheck = 2022;
    for(let i = 0; i < yearCheck; i++)
    console.log(yearCheck)
    if (isLeapYear === 2) {
        //return isLeapYear;
        console.log(isLeapYear)
    }
    
};
leapYears()

// Do not edit below this line
module.exports = leapYears;
